async function fetchDrivers(){const r=await fetch('/api/drivers');const d=await r.json();renderGrid(d);}function renderGrid(d){const t=document.querySelector('#grid tbody');t.innerHTML='';for(const r of d){const e=document.createElement('tr');e.dataset.id=r.id;e.innerHTML=`<td contenteditable="false">${r.first_name}</td><td contenteditable="false">${r.last_name}</td><td><button class="btn-small editBtn">Edit</button></td>`;t.appendChild(e);}}document.getElementById('addBtn').ad...
