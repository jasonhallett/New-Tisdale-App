import { sql } from '../../db.js';
export default async function handler(req,res){try{if(req.method==='GET'){const {rows}=await sql`SELECT * FROM drivers ORDER BY id`;return res.json(rows);}if(req.method==='POST'){const {first_name,last_name}=req.body;const {rows}=await sql`INSERT INTO drivers (first_name,last_name) VALUES (${first_name},${last_name}) RETURNING *`;return res.json(rows[0]);}if(req.method==='PUT'){const {id,first_name,last_name}=req.body;const {rows}=await sql`UPDATE drivers SET first_name=${first_name},last_name=${last_name} WHERE id=${id} RETURNING *`;return res.json(rows[0]);}res.status(405).json({error:'Method not allowed'});}catch(err){console.error(err);res.status(500).json({error:'Server error'});}}